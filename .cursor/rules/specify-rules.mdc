---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---
# specit_trading_test Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-21

## Active Technologies
- C# 12 / .NET 8 (backend), TypeScript 5.x / React 18 (frontend), PowerShell (automation) + ASP.NET Core Minimal APIs, SignalR, EF Core (SqlServer), Auth0 SPA SDK, React Router, Vitest + Testing Library (002-show-user-funds)
- Existing `DemoAccounts` and `DemoHoldings` tables in the market service SQL-backed EF Core store (002-show-user-funds)
- C# 12 / .NET 8 (backend), TypeScript 5.x / React 18 (frontend) + ASP.NET Core Minimal APIs, EF Core (SqlServer), SignalR, Auth0 SPA SDK + JWT bearer APIs, existing `Trading.*` shared libraries (004-trading-pets)
- SQL Server via EF Core (new/extended tables for breeds, pets, supply, listings, bids, trades, notifications); optional JSON/file seed for breed dictionary per spec assumption A-002 (004-trading-pets)
- TypeScript 5.x, React 18 + Vite 6, React Router 6, Auth0 SPA SDK, `@microsoft/signalr`, existing `fetchJson` HTTP helpers (006-pet-trading-ux)
- N/A (client reads market service APIs; server persistence unchanged) (006-pet-trading-ux)
- YAML (GitHub Actions), PowerShell 7 (`pwsh`), Node.js 20, .NET 8 SDK + `actions/checkout`, `actions/setup-node`, `actions/setup-dotnet`, `azure/login` (only in non-PR image job—out of scope to redesign); repo scripts `npm run compose:config`, `npm run infra:validate` (007-fix-github-ci)
- N/A (no data store changes) (007-fix-github-ci)
- TypeScript 5.x, React 18, Vite 6, React Router 6 + Existing `apps/frontend-spa` trading feature (`tradingPetsApi.ts`, `MyPetTraderContext`, `useTradingPetsRealtime` / `hubInvalidateSeq`), Auth0 SPA SDK, `@microsoft/signalr` (008-split-trading-marketplaces)
- N/A (no new persistence; market service remains system of record per spec A-001/A-002) (008-split-trading-marketplaces)
- YAML (GitHub Actions), PowerShell (`pwsh` on `ubuntu-latest`), Bicep (ARM), Azure CLI + `actions/checkout@v4`, `azure/login@v2` (OIDC), Azure CLI (`az`, `az bicep`), GitHub Environments (009-fix-deployment-pipeline)
- N/A for this feature’s code paths; provisioned stack uses Azure SQL, Key Vault, Service Bus, and Container Apps per existing Bicep (009-fix-deployment-pipeline)

- C# 12 / .NET 8 (backend), TypeScript 5.x / React 18 (frontend), Bicep (IaC), PowerShell (automation) + ASP.NET Core Minimal APIs, SignalR, EF Core (SqlServer), Azure Service Bus SDK, Azure Identity, Auth0 SPA + JWT APIs, OpenTelemetry + Application Insights exporter (001-realtime-trading-platform)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

npm test; npm run lint

## Code Style

C# 12 / .NET 8 (backend), TypeScript 5.x / React 18 (frontend), Bicep (IaC), PowerShell (automation): Follow standard conventions

## Recent Changes
- 009-fix-deployment-pipeline: Added YAML (GitHub Actions), PowerShell (`pwsh` on `ubuntu-latest`), Bicep (ARM), Azure CLI + `actions/checkout@v4`, `azure/login@v2` (OIDC), Azure CLI (`az`, `az bicep`), GitHub Environments
- 008-split-trading-marketplaces: Added TypeScript 5.x, React 18, Vite 6, React Router 6 + Existing `apps/frontend-spa` trading feature (`tradingPetsApi.ts`, `MyPetTraderContext`, `useTradingPetsRealtime` / `hubInvalidateSeq`), Auth0 SPA SDK, `@microsoft/signalr`
- 007-fix-github-ci: Added YAML (GitHub Actions), PowerShell 7 (`pwsh`), Node.js 20, .NET 8 SDK + `actions/checkout`, `actions/setup-node`, `actions/setup-dotnet`, `azure/login` (only in non-PR image job—out of scope to redesign); repo scripts `npm run compose:config`, `npm run infra:validate`


<!-- MANUAL ADDITIONS START -->
## Implementation rules
- Always consider using agents from ".cursor/agents/" folder when doing implementation of tasks
<!-- MANUAL ADDITIONS END -->

## Browser login
Use the orlenko.andrew@gmail.com/Hack_1112 values for login to your built-in browser for testing.
