import { test } from '@playwright/test';

test.skip('login -> order -> trade -> settlement smoke', async () => {
  // Backend scaffolding is in place; wire real SPA auth and endpoints before enabling.
});
